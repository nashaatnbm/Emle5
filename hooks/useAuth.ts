/**
 * hooks/useAuth.ts
 *
 * Re-exports useAuth from AuthProvider for convenient imports:
 *   import { useAuth } from "@/hooks/useAuth"
 */

export { useAuth } from "@/components/providers/AuthProvider";
